[Unit]
Description=iCube CRM lesson photo cleanup
After=network.target

[Service]
Type=oneshot
User=icube
Group=icube
WorkingDirectory=/opt/icube-crm
EnvironmentFile=/opt/icube-crm/.env
ExecStart=/usr/bin/npm run photos:cleanup
