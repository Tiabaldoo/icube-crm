{
  "name": "icube-crm",
  "version": "0.2.0",
  "private": true,
  "description": "CRM iCube: frontend prototype, protected business rules and future MySQL API foundation",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check-syntax.mjs",
    "migrate": "node database/migrate.mjs",
    "create-director": "node scripts/create-director.mjs",
    "create-partner": "node scripts/create-partner.mjs",
    "start:api": "node backend/src/server.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "mysql2": "^3.14.4"
  }
}
